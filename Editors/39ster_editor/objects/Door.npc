NPC001
IMAGE door.png
SCRIPTINGLANGUAGE GS1Script

PARAM EXPRESSION hidetime
DEFAULT 3
PARAMEND

SERVERCODE
// NPC made by Stefan Knorr
//#COMMENTS The standard door that hides 3 secs when touched
if (created) {
  setimg door.png;
  setshape 1,32,32;
}
if (playertouchsme) {
  hide;
  sleep param1;
  show;
}
SERVERCODEEND
